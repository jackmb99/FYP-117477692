<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalyearproject117477692">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".Module"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalYearProject117477692">
        <activity android:name=".LeaderboardActivity"></activity>
        <activity android:name=".Testing" />
        <activity android:name=".TestActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Chart" />
        <activity android:name=".Covid" />
        <activity android:name=".EditingGoal" />
        <activity android:name=".EditGoalActivity" />
        <activity android:name=".EditPersonActivity" />
        <activity android:name=".ShowGoals" />
        <activity android:name=".ShowMembers" />
        <activity android:name=".AddToGroup" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>